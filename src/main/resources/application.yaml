spring:
    threads:
        virtual:
            enabled: true
    application:
        name: api
    profiles:
        active: ${SPRING_PROFILES_ACTIVE:local}
    jackson:
        time-zone: Asia/Seoul
        default-property-inclusion: non_null

server:
    servlet:
        context-path: /api
    shutdown: graceful
